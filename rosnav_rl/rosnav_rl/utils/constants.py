"""Static constants for rosnav_rl. Encoder shapes are determined at runtime from
observation space configuration rather than from ROS parameters."""

VALID_CONFIG_NAMES = ["hyperparameters.json", "training_config.yaml"]

REDUCTION_FACTOR = 3
